name: format_then_edit
category: format
language: ts
summary: >
  Branch A reformats a region (changes whitespace, line wraps); branch B
  edits inside the same region. Every line of the function differs between
  branches even though the actual semantic delta is small. git produces
  conflict markers across the whole region.
expected_git_outcome: conflict
expected_sharp_outcome: clean_ok
validator: ts

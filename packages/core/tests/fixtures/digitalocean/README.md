# DigitalOcean fixtures

Recorded JSON responses for DO REST API endpoints used by the
`packages/core/providers/digitalocean` provider. Fixtures are loaded by the
MSW v2 handler in `tests/unit/digitalocean-provision.test.ts`.

These are deliberately minimal — only the fields the provider reads.

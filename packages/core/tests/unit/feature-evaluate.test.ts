import { describe, it } from "vitest";

describe("feature-evaluate blueprint integration (scout)", () => {
  it.todo("initial prompt contains principles, not implementation rules (#81)");
  it.todo("narrow pass introduces implementation rules (#81)");
});

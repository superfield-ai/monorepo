node_modules/
dist/
blueprint/
packages/core/blueprint-data.generated.ts
# Byte-exact golden fixtures for the GHA adapter (issue #823): the emit()
# output is compared byte-for-byte against supported.yml, so prettier must not
# reformat these or the golden round-trip test breaks.
crates/fastenv/tests/fixtures/gha/

# Test fixtures and templates whose content is asserted byte-for-byte or copied
# verbatim by tests; formatting them would break the tests that depend on their
# exact layout (issue #903).
crates/sf-eval/tests/fixtures/corpus/
e2e/fixtures/hello-world/README.md
packages/core/templates/container/README.md
packages/core/tests/fixtures/digitalocean/README.md
tests/fixtures/

# Dev-scout workproduct artifacts: temporary seam reports that gate implementation
# issues. They are not canonical documentation and may have environment-specific
# formatting, so prettier is intentionally skipped for them (issue #903).
docs/scout/*.md

# Deliberately-invalid JSON fixture for the Tier-2 nightly gate selftest
# (issue #864): it exercises the gate script's malformed-input error path, so
# prettier must not try to parse or reformat it.
tests/fixtures/eval-tier2-nightly-gate/malformed.json

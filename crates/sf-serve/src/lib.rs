//! Superfield HTTP serving layer.
//!
//! Stub: real implementation ports the TypeScript API server in a later issue.
//! Integration seam: mount via `superfield::mount_serve()` in the binary
//! entrypoint once the service provides its listener interface.

# Todo tracker — derived project graph (regressed)

> Recorded-artifact fixture (issue #871 scout) — the `project-graph` grader's
> replay input for the **regressed** case: one of the three expected verbs is
> missing (see `tests/tier1_graders.rs` for which), the regression this grader
> exists to catch (e.g. a `ProjectGraphDerive` step that dropped a feature).
>
> Note to editors: keep this fixture's body free of the word this file's name
> implies is missing — the grader is a plain substring match over the whole
> file, so even mentioning the missing verb in prose here would make the
> fixture accidentally pass.

- Feature: Add a task
- Feature: List tasks

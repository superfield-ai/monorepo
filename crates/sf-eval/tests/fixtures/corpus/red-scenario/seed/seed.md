# Seed intent (fixture)

This is a fixture seed intent for `red-scenario`. It is never gardened against
a live loop — it exists only so the scenario-directory discovery contract has
a `.md` file to find under `seed/`.

//! Superfield deploy orchestration.
//!
//! Stub: real implementation ports the deploy pipeline in a later issue.
//! Integration seam: mount via `superfield::mount_deploy()` in the binary
//! entrypoint once the component provides its deploy interface.

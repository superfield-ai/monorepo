[build]
# Persistent per-workspace target dir.  sccache (rustc-wrapper) and the
# sccache disk cache are configured globally in ~/.cargo/config.toml and
# ~/.config/sccache/config.toml respectively.
target-dir = "~/.cache/cargo-target/cli"
